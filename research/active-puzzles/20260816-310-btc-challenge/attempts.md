# Attempts — 310 BTC Challenge

This file preserves experiments rather than deleting failed paths.

## Reconstructed legacy work

The repository already contains evidence that these analysis lanes were attempted:

- alpha-channel LSB extraction
- alpha-value pattern extraction
- alpha 2-bit extraction
- row 310 alpha extraction
- RGB/color statistics
- printable-byte scanning
- per-channel LSB ratio checks
- channel separation / bitplane generation

Exact historical commands, timestamps, and interpretations are not recoverable from filenames alone. Future agents should append reproducible commands and results here rather than infer success from legacy outputs.
